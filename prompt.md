study `${spec}` (if provided)
study `${plan}` and pick the most important thing to do - your task is implement one task and then to validate that this functionality works. If there are no remaining tasks, create a file named `ralph_complete`. Once your task is complete, exit by running: `kill $PPID`

IMPORTANT:
- update the `${plan}` when the task is done
- All tasks in a phase are complete. Mark the phase as complete with `✅ Complete`
- make sure the code passes linting and typechecks