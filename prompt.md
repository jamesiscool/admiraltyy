study `specs/INDEX.md
study `${spec}` (if provided)
study `${plan}` and pick the most important thing to do - your task is implement one task and then to validate that this functionality works. If there are no remaining tasks, create a file named `ralph_complete`. Once your task is complete run `pkill claude` to exit.

IMPORTANT:
- if integration/property/unit tests are missing you should add them (but choose which one is most appropriate. follow existing test pattern design)
- update the `add functional integration tests.md` when the task is done
- All tasks in a phase are complete. Mark the phase as complete with `✅ Complete`
- you may add temporary logging as needed
- make sure the code passes linting and typechecks
- commit when you are done